<h2 class="page-heading">Create a new category</h2>

<%= form_with(model: @category) do |f| %>
  <% if @category.errors.any? %>
  <div style="color: red">
    <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>
    <ul>
      <% @category.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
  <% end %>  
  <div>
    <div class="field">
      <%= f.label :category_name %><br />
      <%= f.text_field :category_name, autofocus: true %>
    </div><br />

    <div class="field form-group col-md-6">
      <%= f.file_field :image, class: "form-control", accept:"image/jpeg, image/jpg" %>
    </div><br />

    <div>
      <%= f.button "Update Category", class: "btn btn-sm btn-primary" %>
    </div><br />
  </div>
<% end %>